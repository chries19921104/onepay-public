<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.admin.mapper.SystemCurrencyMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="org.example.common.entity.SystemCurrency">
        <id column="currency" property="currency" />
        <result column="is_active" property="active" />
        <result column="symbol" property="symbol" />
        <result column="creator" property="creator" />
        <result column="updater" property="updater" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        currency, is_active, symbol, creator, updater, created_at, updated_at
    </sql>

</mapper>
